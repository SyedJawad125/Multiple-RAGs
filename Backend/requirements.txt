# ─── Django core ──────────────────────────────────────────────────
django==5.2.5
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.0
django-cors-headers==4.7.0
django-filter==25.1.0
cryptography==45.0.6
python-dotenv==1.1.1

# ─── Database ─────────────────────────────────────────────────────
psycopg2-binary==2.9.10

# ─── Image handling ───────────────────────────────────────────────
pillow==11.3.0

# ─── Celery + RabbitMQ ────────────────────────────────────────────
celery==5.5.3
pika==1.3.2
django-celery-results>=2.6.0    # unpinned — lets pip find Django 5.2 compatible version
django-celery-beat>=2.7.0       # unpinned — lets pip find Django 5.2 compatible version
amqp==5.3.1
pytz==2025.2
python-dateutil==2.9.0

# ─── API docs ─────────────────────────────────────────────────────
drf-spectacular==0.28.0
drf-spectacular-sidecar==2025.7.1

# ─── CKEditor ─────────────────────────────────────────────────────
django-ckeditor-5==0.2.18

# ─── Production server ────────────────────────────────────────────
gunicorn==23.0.0

# ═════════════════════════════════════════════════════════════════
#  AI Resume Screener (RAG) packages
# ═════════════════════════════════════════════════════════════════

# LLM — Groq
groq>=0.9.0
tenacity>=8.3.0

# LangChain + LangGraph
langchain>=0.2.5
langchain-groq>=0.1.6
langgraph>=0.1.0

# Web search
tavily-python>=0.3.3

# ── PyTorch CPU-only (~200 MB instead of ~2 GB CUDA build) ────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Embeddings
sentence-transformers>=3.0.0

# Vector store
chromadb>=0.5.3

# Resume file parsing
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
python-docx>=1.1.0




# RagStack
# Django Core
Django==5.2.5  # Using newer version from first file
djangorestframework==3.16.1  # Using newer version from first file
django-cors-headers==4.7.0  # Using newer version from first file
django-filter==25.1.0  # Using newer version from first file

# Authentication & Security
djangorestframework_simplejwt==5.5.0  # Using newer version from first file
django-environ==0.11.2
cryptography==45.0.6

# Database
psycopg2-binary==2.9.10  # Using newer version from first file
pgvector==0.2.3

# Caching
django-redis==6.0.0  # Using newer version from first file
redis==5.0.1

# Task Queue & Async
celery==5.5.3  # Using newer version from first file
django-celery-results==2.6.0
pika==1.3.2
amqp==5.3.1

# AI/ML - Embeddings & LLMs
sentence-transformers==2.2.2
transformers==4.35.2
torch==2.1.1
tiktoken==0.5.1

# Search & Ranking
rank-bm25==0.2.2

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
openpyxl==3.1.2
python-pptx==0.6.23
pillow==11.3.0  # From first file (image processing)

# HTTP & API
aiohttp==3.9.1
requests==2.31.0
httpx==0.25.2

# Utilities
python-dotenv==1.1.1  # Using newer version from first file
python-slugify==8.0.1
pytz==2025.2
python-dateutil==2.9.0

# Data Processing
numpy==1.26.2
pandas==2.1.3

# Monitoring & Logging
sentry-sdk==1.38.0

# Production Server
gunicorn==23.0.0  # Using newer version from first file
uvicorn[standard]==0.24.0

# Testing
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
factory-boy==3.3.0

# Code Quality
black==23.11.0
flake8==6.1.0
isort==5.12.0
mypy==1.7.1

# Documentation & API
drf-spectacular==0.26.5
drf-spectacular-sidecar  # From first file, no version specified (latest)

# Rich Text Editor
django-ckeditor-5==0.2.18


huggingface-hub==0.20.3
transformers==4.38.0
sentence-transformers==2.2.2
torch==2.1.0
# pip install huggingface-hub==0.19.4